# Start with an official Node.js image
FROM node:18-alpine

# Set the working directory
WORKDIR /app

# Copy the package.json file to install libraries
COPY package.json .

# Install the Node.js libraries
RUN npm install

# Copy the rest of your app files (server.js, public folder, etc.)
COPY . .

# Tell Docker this app will listen on port 3000
EXPOSE 3000

# The command to run when the container starts
CMD ["node", "server.js"]